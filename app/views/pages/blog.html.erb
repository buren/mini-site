<div class="blog-page-content">
	<%= render 'posts/add_post' %>
	<% @posts.each do |post| %>
		<%= render 'posts/blog_page_post', post: post %>
	<% end %>
	<%= render 'pages/page_actions' %>
	<p class="pagination">
		<%= paginate @posts, window: 2 %>
	</p>
	
</div>